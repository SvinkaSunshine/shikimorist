{
  "name": "shikimorist",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:firefox": "node build.js firefox",
    "build:chrome": "node build.js chrome"
  },
  "devDependencies": {
    "archiver": "^6.0.0"
  }
}